# Current verification evidence — working title

**Current source and package rerun:** 2026-08-27 EDT

**Current sealed-package source status:** 0.2.12 is on OWNER-TEST CANDIDATE HOLD
and contains the current 1,663-test source snapshot plus the compact character-first
desktop presentation, a complete readable scrollable transcript, default always-on-top
behavior in compact and character-only modes, an explicit unpin control, a queued
first-greeting path for the optional authenticated-loopback Chatterbox output route,
and a graphical preserve-or-remove uninstaller. Version 0.2.5 is rejected as
a current owner candidate after two fresh exact-package renderer-start failures;
0.2.6 was an interim repair with stale packaged version wording. Earlier packages
remain historical.

**Sealed-package SHA-256:**
`A99C19ECAF782147FB54AD4CB46D910126A8E8796EC9A98753B0CFBFB5683F65`

**Product identity:** `[FINAL PRODUCT NAME]` remains intentionally unresolved.

This is the sanitized, repository-safe summary for the current source and Windows
candidate. It contains no conversation content, health records, local machine paths,
account identifiers, or credentials.

**Current formal owner-test status:**
[`../verification/FORMAL_OWNER_TEST_STATUS_0.2.12_20260827.md`](../verification/FORMAL_OWNER_TEST_STATUS_0.2.12_20260827.md)
and its adjacent JSON receipt. That status records PASS for the automated software,
exact checksum-verified packaged process, isolated lifecycle, and one bounded
source-provider audible probe. Owner installation and acceptance—including exact
installed-package voice discovery/playback/mute—clinical validation, hackathon
eligibility, publication, and submission remain on HOLD.

## Current source and package gates

- Current source behavior: **1,663/1,663 tests passed** in **32 test files**.
  The ingestion-integrity suite uses a structured actor/evidence/frame/action-sense/
  object/quantity event graph with generated direct-speech, witness, passive,
  preposed-object, future-intent, counterfactual, remote-history, transport/media/task,
  food-object, prescriber-comparison, and persistence probes. Memory regressions cover
  Unicode and honorific relationship names, question/doubt/retraction factuality guards,
  atomic achievement/interest facts, Unicode-punctuation cleanup, appositive family
  identities, separation of transient events from identity, natural-language
  birthday-age capture and correction, complete appointment-provider names,
  general part-of-day medication schedules without invented clock times, and the
  urgent-options action opening the existing disclosure panel without adding a
  transcript turn.
- Independent hostile evaluation: **57/57 scenarios passed**.
- The final focused functional rerun passed **376/376 tests across 11 files**,
  covering long memory, fictional people and relationships, preferences,
  birthdays and corrections, appointments, prescribed-medication routines, regular/
  angry/depressed/immediate-danger conversations, games, privacy, procedural 3D,
  hands-free state handling, the local-voice boundary, and offline behavior.
- Windows desktop shell and installer: **69/69 tests passed**. Added regressions require
  compact and character-only modes to default to always-on-top while full mode does
  not, and require the graphical uninstaller to preserve data by default while keeping
  remove-all explicit. Existing regressions require the fixed-loopback startup retry window
  to outlast the former eight-retry race and attach bounded evidence if that window
  is ever exhausted.
- TypeScript compilation and the Vite production build passed.
- The known-defects inventory is not treated as proof that no defects remain. The
  release and package blockers in this file remain open until independently closed.
- The focused procedural-3D checks passed **18/18**. The exact packaged
  WebGL audit observed one canvas, no companion images, depth testing, the
  head-parent transform hierarchy, advancing motion without renderer reset, and no
  console warnings or errors.
- Social rehearsal tests and live audit cover a fictional introduction, small talk,
  exit, stop, every visible retry/return action, unrelated-language false positives,
  no-persistence of rehearsal identity/interests/birthdays/care plans, and correctly
  scoped safety interruption.
- Safety-integrity regressions verify that disclosed entire-bottle ingestion,
  doubled doses, twice-prescribed amounts, and extra doses route before schedule
  confirmation; acute turns persist no newly extracted medication or appointment
  plans. Prescriber/label language and multi-fact relationship memory variants are
  covered without storing transient arguments as identity.
- These are software and adversarial-scenario tests, **not clinical validation**,
  diagnostic evaluation, or evidence of treatment effectiveness.

## Current Windows candidate

This exact candidate was built from the current 1,663-test source snapshot. The
package receipts bind the exact authored files; a Git revision is not claimed.

- Archive: `Wellbeing-Companion-Working-Title-Setup-0.2.12-win32-x64.zip`
- Size: **150,944,898 bytes**
- SHA-256: `A99C19ECAF782147FB54AD4CB46D910126A8E8796EC9A98753B0CFBFB5683F65`
- Authenticode status: **NotSigned**
- Repeated-run evidence set `20260827T082604418Z-09cb4e84` records **PASS**, **3/3**
  retained distinct runs bound to the same package hash, after one additional direct
  passing verification.
  Each sanitized run started the actual packaged executable and required a native
  window with the working-title identity, custom decoded icon, and tray; a
  fixed-loopback renderer and health response; a local-storage round trip; renderer
  the exact disclosed compatibility state with context isolation and Node integration
  disabled; and genuine
  `webgl-3d-motion` with `motionTick: 15`, `waving: true`, and
  `movementObserved: true`. Each run also denied the bounded hands-free request and
  proved the persistent-partition health warmup and a bounded initial-navigation
  receipt, then completed `Please do not diagnose me.` through the real typed composer with
  the exact deterministic reply, proving that denied/unavailable speech does not
  block text conversation.
- These exact-package runs used checksum-verified payloads extracted from the sealed
  ZIP into fresh isolated roots on Windows build 26200. The bounded Electron GPU and
  renderer child-process compatibility path was active. Context isolation remained
  enabled, Node integration and webviews remained disabled, navigation stayed fixed
  to `127.0.0.1`, and external renderer requests remained blocked. No global
  `--no-sandbox` argument was used. Raw receipts remain local-only; sanitized receipts
  and the summary remove isolated user-data paths.
- The package smokes did not invoke the optional Ollama wording layer or activate the
  heavyweight voice host. Separate integrity checks bind the exact Chatterbox adapter,
  Python host, and reviewed synthetic references while proving no model weights are
  bundled. One source-provider test confirmed audible local playback on this machine;
  the exact installed-package audible route remains an owner-test HOLD.
- The exact 0.2.12 verifier required a PE setup launcher at the extracted root,
  PowerShell support files beneath `Support`, no root `.ps1` files, receipt-only
  verification with no profile mutation, and rejection after a receipt-bound file
  was deliberately altered. The external package receipt also enforces a bounded
  stock-Explorer extraction-path budget.
- Two fresh exact 0.2.5 runs received a healthy private-runtime response but exhausted
  all 25 renderer-navigation attempts with `ERR_FAILED (-2)`. This reproducible
  current failure supersedes the older 0.2.5 pass and prompted the bounded
  compatibility repair. Version 0.2.12 is the sealed successor whose packaged readmes
  match its package identity. The full repair and limitation record is in the current
  formal recheck.

## Lifecycle boundary

The isolated lifecycle harness passed shortcut and Installed-Apps shapes,
preserve-data uninstall, reinstall recovery, and explicit remove-all using the exact
sealed payload beneath a temporary root. It recorded `realUserProfileMutated: false`
and `actualInstallerExecuted: false`. It **does not** replace running the real
installer in a disposable Windows user or virtual machine.

## Product truth boundary

- Typed conversation, local memory, reminders, games, and deterministic safety can
  run locally without an internet connection.
- Hands-free recognition uses Chromium/Web Speech and may be unavailable or may use
  an operating-system or browser-vendor online service. Offline core does not mean
  every speech configuration is offline.
- The normal device-local profile is not described as blanket encrypted storage. A
  user may enable the optional password vault; that protected profile uses
  PBKDF2-SHA-256 and AES-256-GCM. Primary and guardian vault roles are separated.
- Optional Ollama wording is loopback-only and limited to ordinary steady
  conversation. Protected routes remain deterministic.
- No clinical validation, diagnosis, prescribing, dose changes, emotion-reading,
  or guaranteed crisis outcome is claimed.

## Remaining release and submission gates

- Obtain written Hack for Humanity organizer confirmation that AI-assisted
  development is compatible with the rule prohibiting development assistance from
  outside the registered team. Until then, this is a product candidate, not a
  verified eligible hackathon submission.
- `[FINAL PRODUCT NAME]`, `[OWNER NAME]`, `[GITHUB URL]`,
  `[HOSTED OR DEMO URL]`, and `[VIDEO URL]` must be resolved by the owner.
- The owner must run the exact candidate and complete the planned install, preserve,
  reinstall, and remove-all checks in a disposable Windows user or virtual machine.
- A normal public Windows release needs publisher signing; this archive must remain
  labeled as an unsigned test candidate until then.
- Independent accessibility review of the exact current procedural-3D package remains
  pending; automated accessibility regressions do not replace that review.
- The public repository, downloadable judge path, final product captures, accessible
  video at four minutes or less, retimed English captions, link checks, and final
  owner approval are still pending.
- Any source change selected for submission requires a rebuild, three packaged
  smokes, the lifecycle harness, a new checksum seal, and replacement of the exact
  package size and checksum everywhere before recording.
- `desktop-shell/scripts/Run-ThreePackagedSmokes.ps1 -RunCount 3` preserved each
  packaged invocation under a distinct run ID, retained raw evidence locally,
  emitted a path-sanitized receipt for sharing, and wrote PASS only after three
  distinct runs bound to the same package hash. The current retained set is
  `20260827T082604418Z-09cb4e84`.

Historical receipts under the local verification archive remain useful for audit
history but do not supersede the current 1,663-test source and 0.2.12 package seal.
Earlier archives and their receipts remain preserved as historical evidence.
